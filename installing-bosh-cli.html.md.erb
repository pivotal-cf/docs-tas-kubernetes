---
title: Installing the BOSH CLI
owner: Tanzu Application Service Release Engineering
---

This topic describes how to install the BOSH Command Line Interface tool (`bosh`).

To install `bosh`, follow the procedures for your operating system to obtain it from [VMware Tanzu Network](https://network.pivotal.io/products/tas-for-kubernetes). Binaries are provided only for 64-bit architectures.



##<a id='mac'></a> macOS

<ol><li>Navigate to <a href="https://network.pivotal.io/">VMware Tanzu Network</a> and log in.</li>
<li>Click <strong>VMware Tanzu Application Service for Kubernetes</strong>.</li>
<li>Click <strong>BOSH CLI</strong>.</li>
<li>Click <strong>BOSH CLI for macOS</strong> to download the BOSH CLI binary.</li>
<li>Rename the downloaded binary to <code>bosh</code>.</li>
<li>On the command line, run the following command to make the BOSH CLI binary executable:
  <pre class="terminal">$ chmod +x bosh</pre></li>
<li>Move the binary into your <code>PATH</code>. For example:
  <pre class="terminal">$ mv bosh /usr/local/bin/bosh</pre></li>
</ol>


##<a id='linux'></a> Linux

<ol><li>Navigate to <a href="https://network.pivotal.io/">Pivotal Network</a> and log in.</li>
<li>Click <strong>VMware Tanzu Application Service for Kubernetes</strong>.</li>
<li>Click <strong>BOSH CLI</strong>.</li>
<li>Click <strong>BOSH CLI for Linux</strong> to download the BOSH CLI binary.</li>
<li>Rename the downloaded binary to <code>bosh</code>.</li>
<li>On the command line, run the following command to make the BOSH CLI binary executable:
  <pre class="terminal">$ chmod +x bosh</pre></li>
<li>Move the binary into your <code>PATH</code>. For example:
  <pre class="terminal">$ mv bosh /usr/local/bin/bosh</pre></li>
</ol>

##<a id='windows'></a> Windows

<ol><li>Navigate to <a href="https://network.pivotal.io/">Pivotal Network</a> and log in.</li>
<li>Click <strong>VMware Tanzu Application Service for Kubernetes</strong>.</li>
<li>Click <strong>BOSH CLI</strong>.</li>
<li>Click <strong>BOSH CLI for Windows</strong> to download the BOSH CLI executable file.</li>
<li>Rename the downloaded binary to <code>bosh.exe</code>.</li>
<li>Move the binary into your <code>PATH</code>.</li>
</ol>